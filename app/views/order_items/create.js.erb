// $("#cart_items_total").html("<%= j render(partial: 'create') %>");

// $("input#order_item_quantity").each(function( index ) {
//   $(this).val(null)
// });

$('.right-col').html("<%= j render(partial: '/carts/show') %>");
alert("You have reached the right AJAX file");
